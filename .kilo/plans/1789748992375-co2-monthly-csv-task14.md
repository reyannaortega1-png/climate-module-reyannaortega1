# Task 1.4 — Load the monthly CSV distribution

## Context

- `climate.ipynb` Task 1.4 markdown cell (id `ae0897ec`) is followed by an empty code cell
  (id `cf17c087`) and a `**Your answer:**` markdown cell (id `7fc53631`).
- Alternative distribution from <https://gml.noaa.gov/ccgg/trends/data.html>:
  `https://gml.noaa.gov/webdata/ccgg/trends/co2/co2_mm_mlo.csv` — verified live 2026-09-18:
  - Identical `#` prose header block (license + SIO/interpolation/eruption notes), then a real
    named header row: `year,month,decimal date,average,deseasonalized,ndays,sdev,unc`.
  - 822 data rows (Mar 1958 – Aug 2026), same values as the `.txt`.
  - Same sentinels: 194 SIO rows (Mar 1958–Apr 1974) with `-1,-9.99,-0.99`; interpolated
    Dec 1975 (`ndays=-1, sdev=-9.99, unc=0.00`) and Apr 1984 (`ndays=2, sdev=-9.99, unc=0.00`)
    → `sdev < 0` matches exactly 196 rows, same as Task 1.2/1.3 findings.
  - Eruption NOTE still only in `#` comments; Dec 2022–Jul 2023 rows still fully normal.

## Cells to insert (between id `ae0897ec` and `7fc53631`; replace empty cell `cf17c087`)

Unique fresh ids (e.g. `d14a5b11`–`d14a5b15`), code cells `execution_count: null`,
`outputs: []`, `metadata: {}` — same schema as the Task 1.3 cells. Keep JSON round-trip safe
(`json.dumps(indent=1, ensure_ascii=False)`).

1. md label: "Load the CSV version — the columns tell you their own names"
   ```python
   co2_csv = pd.read_csv("https://gml.noaa.gov/webdata/ccgg/trends/co2/co2_mm_mlo.csv", comment = '#')
   co2_csv.head()
   ```
2. md label: "Checking whether switching formats fixed the flags: rows with a negative `sdev`"
   ```python
   co2_csv[co2_csv.sdev < 0]
   ```
   (822-row frame; this displays all 196 flagged rows — keep as displayed evidence. If the
   196-row dump is considered too long, use
   `co2_csv[co2_csv.sdev < 0].groupby(co2_csv.year < 1975).size()` instead; default: show rows,
   it is the direct evidence.)
3. Answer cell (replace `**Your answer:**` source of `7fc53631`, student's own voice, matching
   the 1.2/1.3 answers):

   Solved: the column names — CSV carries a real header row
   (`decimal date`, `average`, `deseasonalized`, `ndays`, `sdev`, `unc`), so no hand-written
   `columns` list, nothing mislabeled as "smooth"/"empty", no bogus 8th blank column
   (Task 1.1 problems). Not solved: the data problems — same negative placeholders on all SIO
   rows, same two interpolated months flagged only by negative sdev (still 196 negative-flag
   rows overall), same gap-free-looking series, and the prose notes (SIO credit, interpolation
   policy, eruption site swap) are still `#` comment lines that `comment='#'` discards.
   Switching format fixed my parser, not the data.

## Constraints

- Rubric: no `print`/`try`/comments, one task per cell, markdown label above each, display via
  auto-print. `pandas` is already imported in cell 1; `read_csv` takes URLs directly (matches
  existing Part 1 pattern, so CI needs no extra deps).
- Note the space in the `decimal date` column name if any later cell needs it
  (`co2_csv['decimal date']`) — the planned cells don't.

## Validation

- Round-trip check file formatting; `nbformat.validate` after edit (same approach as Task 1.3,
  edit cells by id, recompute indexes AFTER insertion — the Task 1.3 patch hit a stale-index bug).
- Execute the two code cells standalone against the live URL: expect header row parsed into 8
  named columns (822 rows) and exactly 196 rows with `sdev < 0` (194 pre-1975 + Dec 1975 + Apr 1984).
